# Projeto Servlets com Docker

## Como rodar

1. Compile o projeto:
   mvn clean package

2. Suba os containers:
   docker-compose up --build

3. Acesse:
   http://localhost:8080/hello
   http://localhost:8080/you?nome=SeuNome
